/** 全站品牌名（页脚、OG site_name、组织型 JSON-LD 等） */
export const SITE_NAME = 'Windrose Compass'

/**
 * 浏览器标签标题左侧用语（突出游戏名，利于搜索）；完整标题为
 * `{@link DOCUMENT_TITLE_LEAD} | {描述段}`，描述段来自路由 `meta.title` 或文章 `seo.title`。
 */
export const DOCUMENT_TITLE_LEAD = 'Windrose Game'
