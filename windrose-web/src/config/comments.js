/** Public comment API (see repo `接入需求描述.md`). Browser requests must use an Origin allowed in the project dashboard. */
export const COMMENT_API_ORIGIN = 'https://comment-api-pi.vercel.app'

export const COMMENT_PROJECT_SLUG = 'windrosegame'

/** Home page section (`SINGLE` template, `ratingMode: NONE`). */
export const COMMENT_HOME_SECTION_SLUG = 'home'
